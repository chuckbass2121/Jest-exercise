import Util from './util';

let util;
beforeEach(() => {
	util = new Util();
});

test('test a method', () => {
	// expect(util.a()).toBe();
});

test('test b method', () => {
	// expect(util.b()).toBe();
});
